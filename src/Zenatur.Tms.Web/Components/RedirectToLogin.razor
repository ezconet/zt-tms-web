@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Página de bloqueio simples: o Silent Login real é POST do legado.
        // Aqui apenas mostra mensagem clara. No futuro, redirecionar para sistema legado.
        // relativo: resolve contra <base href> (PathBase /tmsweb)
        Nav.NavigateTo("auth/required", forceLoad: false);
    }
}
